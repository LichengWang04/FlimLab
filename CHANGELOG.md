# Changelog

本文件记录面向用户的变更。

## 1.0.0-beta.2 — 2026-08-30

- 公共测试发布改为明确的 Windows x64 未签名策略：Release 页面、安装说明与构建元数据均标记 `NotSigned`，并通过 SHA-256 校验文件建立下载完整性边界。
- Sony A7R V ARW 私有样张集扩充到 6 张；ARW 与尚无真实样张的 CR2/NEF/RW2 在公开兼容性说明中分开标注。
- 新增 CR2、NEF、RW2、ARW 相机原始文件导入：内置 LibRaw 0.22.1 WebAssembly 解码器，以 16 位线性 sRGB 贯通缩略图、1600 px 预览和全分辨率导出；JPEG/PNG/TIFF 既有路径保持不变。当前真实相机验收只覆盖 Sony A7R V ARW 的元数据与 1600 px 解码。
- 新增 WebGPU 预览调色路径：场景线性缓存上传一次后，曝光、对比度、高光压缩和饱和度改为 WGSL uniform 热更新，经典引擎与“相纸反相 5.6”引擎共用该路径。WebGPU 不可用时回退 Canvas2D/CPU，导出继续使用确定性的 CPU Worker。
- RAW 新增 256 MiB 文件、70 MP 像素与 WASM 解码峰值内存边界；真实 Sony ARW 覆盖元数据与预览验收，全尺寸导出在内存不足时按设计提前拒绝，并增加独立 GPU 冒烟入口。
- 安装包新增 libraw-wasm、LibRaw 与 Little CMS 的运行时解包规则、完整许可证和 CDDL 源码取得说明。
- 新增独立“相纸反相 5.6”处理引擎，冻结到 darktable 5.6.0 negadoctor 的默认值、参数范围和片基密度/相纸打印/高光软压缩结构；历史配方继续迁移为经典引擎。
- 新引擎默认在线性 Rec.2020 中处理并回到线性 sRGB 导出，提供彩负/黑白预设、片基与三类分析 ROI，以及可复现的一次性稳健自动设置。
- 同一像素核贯通同步预览、Web Worker、并行导出 Worker 与故障回退，并新增公式 oracle、色彩空间、配方迁移和 darktable CLI 私有样张验收入口。
- 性能优化保持逐字节输出不变：8/16 位 sRGB 量化改用精确 Float32 阈值表，JPEG/PNG 解码改用整数反传递函数表；相纸反相预览缓存几何、工作色域和片基准备结果。
- 16 位 TIFF 预览改为按 strip 直接生成精确面积平均缩图，避免完整 Float32 源栅格；大图导出按像素量动态使用最多 10 个 Worker，并并发压缩有序 TIFF strips。24 MP 基准的 canonical 与并行输出逐字节一致。

## 1.0.0-beta.1 — 2026-08-17

- Windows 公测收口：新增输入文件、像素、边长、整卷帧数、TIFF strip/IFD 与预计内存上限；IPC 对导入模式、frame ID、Recipe、ROI、格式和数组长度执行主进程验证。
- 导出前检查目录与可用空间，OOM、磁盘写满、损坏 TIFF 和非法请求返回明确错误；删除帧、重新导入、导出完成及窗口销毁会释放路径和取消状态。
- 增加最近会话自动恢复、异常退出恢复、只含去路径化信息的诊断复制，以及 IPC/缓存/Worker 重启/路径生命周期发布回归。
- 补齐 NOTICE、隐私说明、完整 Windows 运行时许可证清单和安装包内许可证文本；应用“关于”页标注支持入口与本地处理策略。
- 新增 Windows 签名发布流水线：干净 checkout 执行 `npm ci`、测试、构建、打包，验证安装器/主程序/卸载器签名与时间戳，执行静默安装 smoke、卸载、SHA-256 和发布元数据上传。
- 公测已知限制明确记录：本轮不建立真实底片验收集；无色卡单帧全局估计、未标记 16-bit TIFF 线性假设、混合光、严重主色、染料退化和特殊扫描曲线不作为首版阻塞项。

- 预览还原和缩略图移入长驻 Web Worker；连续滑杆请求只发布最新 revision，当前帧优先，Worker 异常自动回退同步 CPU 路径。最多缓存三帧源图，只有当前帧保留派生缓存，预算 192 MiB。
- 新增五层 `NegativeSession` 缓存：几何、片基/密度/去色罩、反转、白平衡和调色按字段精确失效；调色热更新不再重复片基估计、中性轴/PCA/曲线拟合、反转和白平衡。
- 全分辨率导出移入独立协调 Worker，大图在最多四个 Node Worker Threads 间按 128 行分块；整卷仍逐帧处理。子线程失败回退无状态单线程核心，协调 Worker 崩溃只重启一次，退出时统一终止。
- 密度、反转、增益、调色和编码改用共享区间内核；调色与 sRGB 量化融合，同时通过 `Math.fround` 保留原有 Float32 栅格边界。快速选择替代百分位完整排序，最终 8/16 位结果有 1/2/4 分块逐字节一致性测试。
- JPEG 与 TIFF 均使用临时文件原子发布；新增 `npm run benchmark` 记录 1600px 冷/热预览和 24MP 导出，不设置易波动的 CI 时间断言。本轮有意不采用 GPU。
- 前端升级为暖色专业暗房风，新增胶卷暗盒应用图标；单帧隐藏冗余胶片条，顶栏按导入、当前帧导出和整卷导出重新分组。
- “打开负片/导入整卷”合并为“导入底片”，同一文件对话框自由选择一张或多张，文件夹导入保持独立。
- 旋转改为在画面上绘制水平参考线，支持任意角度双线性校正和最大内接自动裁切；应用角度时清除已失效的裁剪、片基和中性选区。
- 删除手动白平衡 R/B，关闭灰世界自动白平衡后可调 2500–10000K 色温，5500K 为中性零点。
- 自动中和新增稳健 PCA 中性主轴候选及 12 段 PAVA 单调通道曲线，通过确定性 80/20 空间留出验证选择方法；曲线须使验证残差改善至少 10% 且不恶化尾部才启用，可进一步减少阴影与高光方向不同的残留色罩。
- 曲线外推斜率限制为 0.25–4、额外校正限制为 ±0.35D；支撑、跨度或验证收益不足时依次回退到最佳仿射轴、单锚点或单位校正。状态栏显示当前方法、验证改善率和仿射斜率。
- 自动中和保留按影调分层的两轮中性轴拟合,在 0.2–2.5D 范围内稳健估计通道偏移与斜率,并在归一化通道响应之后应用密度预饱和。
- 整卷工作流:一次导入整卷胶片(多选文件或文件夹),胶片条展示每帧缩略图;每帧独立配方,可单独旋转/裁剪/调色,「应用到整卷」一键复制;逐帧跳过与移除。
- 整卷导出:TIFF/JPEG 顺序批量导出到所选文件夹,进度实时显示、可取消,单帧失败不中断并汇总报告,同名自动加后缀。
- 自动中和升级为逐通道仿射拟合(分箱中位数 + Theil–Sen 稳健回归),同时吸收片基残余偏移与通道对比度差异,中性色在整个影调范围保持一致;单锚点方法保留为回退,近似恒等拟合被抑制。
- 新增自动白平衡:灰世界中位数估计三通道增益(限幅 ±2 档),默认开启;状态栏显示自动增益。

## 1.0.0 — 2026-08-15

彻底重写精简版。以「负片扫描 → 还原正像」为核心,丢弃旧版的 RAW 解码、标定配置、DNG/HEIF、批处理与项目系统,换取一个可维护的小代码库。

- 核心管线:线性透射 → 片基检测(默认自动上包络估算,框选未曝光边缘获得实测值)→ 相对密度 → Dmin/Dmax 锚点 + 中性高密度通道范围(自动中和橙罩)→ 10^D 反转 → 线性域调色 → 白点归一化 → sRGB 编码。
- 输入:8/16 位 TIFF(自有解码器,支持 Deflate/LZW/PackBits/无压缩)、JPEG、PNG;带 ICC 的 TIFF 明确拒绝并给出转换提示。
- 交互:画布实时预览、旋转/裁剪、片基与中性区框选、白平衡/预饱和/Dmax/曝光/对比度/高光/饱和度控件;片基采样失败自动回退自动估算。
- 导出:16-bit sRGB TIFF(自有编码器,原子发布)与质量 95 JPEG,全分辨率重算。
- 验证:68 项测试覆盖核心数学、端到端反转、文件解码与导出回读;无头烟雾测试验证应用启动。
